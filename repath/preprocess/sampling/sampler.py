from abc import ABCMeta, abstractmethod
from typing import Tuple

from repath.preprocess.patching.patch_index import PatchIndex

def sample(index: PatchIndex, num_samples: int) -> PatchIndex:
    pass